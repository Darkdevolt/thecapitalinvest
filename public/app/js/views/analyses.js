// ═══════════════════════════════════════════════════════════════
// VIEW — RECOMMANDATIONS / FICHES D'ANALYSE CLIENT
// Source de données : analyses + cours + financials + dividendes + entreprises.
// Aucun nouveau endpoint ni aucune modification de schéma.
// ═══════════════════════════════════════════════════════════════

function ensureRecommendationStyles(){
  if(document.getElementById('tc-recommendations-client-css'))return;
  const link=document.createElement('link');link.id='tc-recommendations-client-css';link.rel='stylesheet';link.href='app/css/recommendations-client.css?v=1';document.head.appendChild(link);
}
function analyseRecType(value){const rec=String(value||'').toLowerCase();if(rec.includes('achat')||rec.includes('buy')||rec.includes('renforcer'))return'buy';if(rec.includes('vend')||rec.includes('sell')||rec.includes('alléger'))return'sell';return'hold';}
function analyseRecLabel(type){return{buy:'Acheter',sell:'Vendre',hold:'Conserver'}[type]||'Conserver';}
function analyseSafeText(value){return String(value==null?'':value);}
function analyseEsc(value){return analyseSafeText(value).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));}
function analyseNum(value){const n=Number(value);return Number.isFinite(n)?n:null;}
function analyseFmt(value){const n=analyseNum(value);return n===null?'—':fmt(n);}
function analyseFmtM(value){const n=analyseNum(value);return n===null?'—':fmtM(n);}
function analyseDate(value){return value?fmtDate(value):'—';}
function analyseTicker(t){return analyseSafeText(t).trim().toUpperCase();}
function analyseCompany(ticker){const t=analyseTicker(ticker);return(Array.isArray(allEntreprises)?allEntreprises:[]).find(e=>analyseTicker(e?.ticker)===t)||{};}
function analyseQuote(ticker){const t=analyseTicker(ticker);return(Array.isArray(allCours)?allCours:[]).find(c=>analyseTicker(c?.ticker)===t)||{};}
function analyseFinancials(ticker){const t=analyseTicker(ticker);return(Array.isArray(allFinancials)?allFinancials:[]).filter(f=>analyseTicker(f?.ticker)===t&&String(f?.validation_status||'draft').toLowerCase()!=='rejected').sort((a,b)=>{const ay=Number(a?.annee||0),by=Number(b?.annee||0);if(by!==ay)return by-ay;const order={annuel:9,TTM:8,S2:7,S1:6,T4:5,T3:4,T2:3,T1:2};return(order[String(b?.periode||'annuel')]||1)-(order[String(a?.periode||'annuel')]||1);});}
function analyseDividends(ticker){const t=analyseTicker(ticker);return(Array.isArray(window.allDividendes)?window.allDividendes:[]).filter(d=>analyseTicker(d?.ticker)===t).sort((a,b)=>Number(b?.annee||b?.exercice||0)-Number(a?.annee||a?.exercice||0));}
function analyseTarget(a){return analyseNum(a?.cours_cible)!==null?analyseNum(a.cours_cible):analyseNum(a?.objectif_cours);}
function analyseReference(a,quote){return analyseNum(a?.cours_reference)!==null?analyseNum(a.cours_reference):analyseNum(quote?.cours);}
function analysePotential(a,quote){const explicit=analyseNum(a?.potentiel_pct);if(explicit!==null)return explicit;const target=analyseTarget(a),ref=analyseReference(a,quote);return target!==null&&ref!==null&&ref>0?((target-ref)/ref)*100:null;}
function analysePeriodLabel(f){const p=String(f?.periode||'annuel');return`${f?.annee||'—'} · ${({annuel:'Annuel',S1:'S1',S2:'S2',T1:'T1',T2:'T2',T3:'T3',T4:'T4',TTM:'TTM'}[p]||p)}`;}
function analyseMetric(label,value,note=''){return`<div class="tc-rec-metric"><span>${analyseEsc(label)}</span><strong>${analyseSafeText(value)}</strong>${note?`<small>${analyseEsc(note)}</small>`:''}</div>`;}
function analyseRecoBadge(a,large=false){const type=analyseRecType(a?.recommandation),label=analyseSafeText(a?.recommandation||analyseRecLabel(type));return`<span class="tc-rec-badge ${type}${large?' large':''}">${analyseEsc(label)}</span>`;}
function analyseReviewAge(a){if(!a?.date_analyse)return'';const age=Math.round((Date.now()-Date.parse(String(a.date_analyse).slice(0,10)+'T12:00:00'))/86400000);if(age>365)return'Note de plus d’un an';if(age>180)return'Note de plus de six mois';return'';}
function analyseReviewFlag(a,quote){const target=analyseTarget(a),price=analyseNum(quote?.cours),reasons=[];if(target!==null&&price!==null){const type=analyseRecType(a.recommandation);if((type==='buy'&&price>=target)||(type==='sell'&&price<=target))reasons.push('objectif atteint');}const age=analyseReviewAge(a);if(age)reasons.push(age);if(!a?.commentaire)reasons.push('analyse non renseignée');return reasons;}
function renderAnalyseCard(a,clickable=true){const quote=analyseQuote(a.ticker),company=analyseCompany(a.ticker),target=analyseTarget(a),ref=analyseReference(a,quote),potential=analysePotential(a,quote),financials=analyseFinancials(a.ticker),latest=financials[0],review=analyseReviewFlag(a,quote),name=company.nom||company.raison_sociale||a.ticker||'—',title=`${a.ticker||'—'} · ${analyseSafeText(a.recommandation||'Analyse')}`,clickAttr=clickable?`onclick="openAnalyseDetail(${Number(a.id)||0})"`:'';return`<article class="tc-rec-card" ${clickAttr}><div class="tc-rec-card-top"><div><div class="tc-rec-kicker">${analyseEsc(a.ticker||'—')}</div><h3>${analyseEsc(name)}</h3><div class="tc-rec-note-title">${analyseEsc(title)}</div></div>${analyseRecoBadge(a)}</div><div class="tc-rec-price-row"><div><span>Cours actuel</span><strong>${analyseFmt(ref)} <small>FCFA</small></strong></div>${target!==null?`<div><span>Objectif</span><strong class="gold">${analyseFmt(target)} <small>FCFA</small></strong></div>`:''}${potential!==null?`<div><span>Potentiel</span><strong class="${potential>=0?'positive':'negative'}">${potential>=0?'+':''}${potential.toFixed(1)}%</strong></div>`:''}</div><div class="tc-rec-fin-row">${latest?`<div><span>Dernier exercice</span><strong>${analyseEsc(analysePeriodLabel(latest))}</strong></div><div><span>Résultat net</span><strong>${analyseFmtM(latest.resultat_net)}</strong></div><div><span>CA</span><strong>${analyseFmtM(latest.chiffre_affaires)}</strong></div>`:''}</div>${a.commentaire?`<p class="tc-rec-comment">${analyseEsc(a.commentaire).slice(0,280)}${String(a.commentaire).length>280?'…':''}</p>`:''}<div class="tc-rec-footer"><span>${a.date_analyse?`📅 ${analyseDate(a.date_analyse)}`:''}${a.horizon?` · ${analyseEsc(a.horizon)}`:''}</span>${review.length?`<span class="tc-rec-review">${analyseEsc(review[0])}</span>`:'<span class="tc-rec-current">Analyse disponible</span>'}</div></article>`;}
function renderAnalysesDashboard(rows){const data=Array.isArray(rows)?rows:[],counts={buy:0,sell:0,hold:0};data.forEach(a=>counts[analyseRecType(a.recommandation)]++);const potentials=data.map(a=>analysePotential(a,analyseQuote(a.ticker))).filter(v=>v!==null),avg=potentials.length?potentials.reduce((x,y)=>x+y,0)/potentials.length:null,latest=data.slice().sort((a,b)=>Date.parse(b.date_analyse||0)-Date.parse(a.date_analyse||0))[0];return`<section class="tc-rec-dashboard"><div class="tc-rec-dashboard-head"><div><div class="tc-rec-kicker">THE CAPITAL · RESEARCH</div><h2>Recommandations d’investissement</h2><p>Des convictions transformées en fiches d’analyse prêtes à être lues, imprimées et transmises à un client.</p></div>${latest?`<div class="tc-rec-last"><span>Dernière note</span><strong>${analyseDate(latest.date_analyse)}</strong></div>`:''}</div><div class="tc-rec-kpis">${analyseMetric('Notes publiées',data.length)}${analyseMetric('Acheter / Renforcer',counts.buy)}${analyseMetric('Conserver',counts.hold)}${analyseMetric('Alléger / Vendre',counts.sell)}${analyseMetric('Potentiel moyen',avg===null?'—':`${avg>=0?'+':''}${avg.toFixed(1)}%`)}</div></section>`;}
function renderAnalyses(){ensureRecommendationStyles();window._analyseRows=Array.isArray(allAnalyses)?allAnalyses.slice():[];filterAnalyses();}
function setAnalyseFilter(f,btn){_analyseFilter=f;document.querySelectorAll('#view-analyses .filter-btn').forEach(b=>{b.classList.remove('active');b.setAttribute('aria-pressed','false');});if(btn){btn.classList.add('active');btn.setAttribute('aria-pressed','true');}filterAnalyses();}
function filterAnalyses(){ensureRecommendationStyles();const q=(document.getElementById('searchAnalyses')?.value||'').trim().toLowerCase();let rows=Array.isArray(window._analyseRows)?window._analyseRows.slice():[];if(_analyseFilter!=='all')rows=rows.filter(r=>analyseRecType(r.recommandation)===_analyseFilter);if(q)rows=rows.filter(r=>[r.ticker,r.analyste,r.recommandation,r.horizon,r.commentaire].map(analyseSafeText).join(' ').toLowerCase().includes(q));rows.sort((a,b)=>Date.parse(b.date_analyse||0)-Date.parse(a.date_analyse||0));const list=document.getElementById('analysesList');if(!list)return;let dashboard=document.getElementById('analysesDashboard');if(!dashboard){dashboard=document.createElement('div');dashboard.id='analysesDashboard';list.parentNode.insertBefore(dashboard,list);}dashboard.innerHTML=renderAnalysesDashboard(rows);list.innerHTML=rows.length?`<div class="tc-rec-grid">${rows.map(a=>renderAnalyseCard(a,true)).join('')}</div>`:'<div class="empty-state"><div class="empty-icon">◎</div><div class="empty-title">Aucune analyse</div><div class="empty-text">Aucune recommandation ne correspond aux critères sélectionnés.</div></div>';}
function latestPublishedRows(ticker){return analyseFinancials(ticker).slice(0,5);}
function financialTableForRecommendation(ticker){const rows=latestPublishedRows(ticker);if(!rows.length)return'<div class="tc-rec-empty">Aucune donnée financière publiée disponible.</div>';return`<div class="tc-rec-table-wrap"><table class="tc-rec-table"><thead><tr><th>Période</th><th class="right">Chiffre d’affaires</th><th class="right">Résultat net</th><th class="right">BPA</th><th class="right">DPA</th><th class="right">Marge nette</th><th>Statut</th></tr></thead><tbody>${rows.map(f=>{const margin=analyseNum(f.marge_nette)!==null?analyseNum(f.marge_nette):(analyseNum(f.chiffre_affaires)&&analyseNum(f.resultat_net)!==null?Number(f.resultat_net)/Number(f.chiffre_affaires)*100:null);return`<tr><td><strong>${analyseEsc(analysePeriodLabel(f))}</strong></td><td class="right">${analyseFmtM(f.chiffre_affaires)}</td><td class="right strong">${analyseFmtM(f.resultat_net)}</td><td class="right">${analyseFmt(f.bpa)}</td><td class="right">${analyseFmt(f.dpa)}</td><td class="right">${margin===null?'—':margin.toFixed(1)+'%'}</td><td>${analyseEsc(String(f.validation_status||'draft')==='validated'?'Validé':'En validation')}</td></tr>`;}).join('')}</tbody></table></div>`;}
function publicationSummaryForRecommendation(ticker){const fins=latestPublishedRows(ticker),divs=analyseDividends(ticker).slice(0,3);if(!fins.length&&!divs.length)return'<div class="tc-rec-empty">Aucun élément publié supplémentaire disponible.</div>';const items=[];fins.slice(0,3).forEach(f=>items.push(`<div class="tc-rec-pub-item"><span class="tc-rec-pub-type">ÉTAT FINANCIER</span><strong>${analyseEsc(analysePeriodLabel(f))}</strong><span>${analyseEsc(f.source||'Source The Capital')}</span>${f.source_url&&/^https?:\/\//i.test(f.source_url)?`<a href="${analyseEsc(f.source_url)}" target="_blank" rel="noopener noreferrer">Source ↗</a>`:''}</div>`));divs.forEach(d=>items.push(`<div class="tc-rec-pub-item"><span class="tc-rec-pub-type">DIVIDENDE</span><strong>Exercice ${analyseEsc(d.annee||d.exercice||'—')}</strong><span>${analyseFmt(d.montant||d.montant_net)} FCFA · ${analyseEsc(d.statut||'Publié')}</span></div>`));return`<div class="tc-rec-pub-list">${items.join('')}</div>`;}
function analysisClientSheet(a){const ticker=analyseTicker(a.ticker),company=analyseCompany(ticker),quote=analyseQuote(ticker),fins=analyseFinancials(ticker),latest=fins[0]||{},prev=fins[1]||{},target=analyseTarget(a),ref=analyseReference(a,quote),potential=analysePotential(a,quote),name=company.nom||company.raison_sociale||ticker,sector=company.secteur||company.sous_secteur||'—',rn=analyseNum(latest.resultat_net),prevRn=analyseNum(prev.resultat_net),rnGrowth=rn!==null&&prevRn!==null&&prevRn!==0?((rn-prevRn)/Math.abs(prevRn))*100:null,generated=new Date();return`<div class="tc-client-sheet" id="tcClientSheet"><header class="tc-client-head"><div><div class="tc-client-brand">THE · CAPITAL</div><div class="tc-client-sub">INTELLIGENCE FINANCIÈRE AFRICAINE</div></div><div class="tc-client-date">FICHE D’ANALYSE<br><strong>${analyseDate(a.date_analyse||generated.toISOString())}</strong></div></header><section class="tc-client-hero"><div><span class="tc-client-kicker">${analyseEsc(ticker)} · ${analyseEsc(sector)}</span><h1>${analyseEsc(name)}</h1><p>Note de recherche · Horizon ${analyseEsc(a.horizon||'non précisé')}</p></div>${analyseRecoBadge(a,true)}</section><div class="tc-client-market">${analyseMetric('Cours de référence',ref===null?'—':`${analyseFmt(ref)} FCFA`,quote?.date_seance?`Séance ${analyseDate(quote.date_seance)}`:'')}${analyseMetric('Objectif',target===null?'—':`${analyseFmt(target)} FCFA`)}${analyseMetric('Potentiel',potential===null?'—':`${potential>=0?'+':''}${potential.toFixed(1)}%`)}${analyseMetric('Dernier exercice',latest.annee?analysePeriodLabel(latest):'—')}</div><section class="tc-client-section"><div class="tc-client-section-title">01 · THÈSE D’INVESTISSEMENT</div><div class="tc-client-thesis">${a.commentaire?analyseEsc(a.commentaire).replace(/\n/g,'<br>'):'Aucune thèse détaillée n’a été renseignée dans la note.'}</div></section><section class="tc-client-section"><div class="tc-client-section-title">02 · DERNIÈRES DONNÉES FINANCIÈRES PUBLIÉES</div><div class="tc-client-kpi-grid">${analyseMetric('Chiffre d’affaires',analyseFmtM(latest.chiffre_affaires))}${analyseMetric('Résultat net',analyseFmtM(latest.resultat_net),rnGrowth===null?'':`${rnGrowth>=0?'+':''}${rnGrowth.toFixed(1)}% vs précédent`)}${analyseMetric('BPA',analyseFmt(latest.bpa),'FCFA')}${analyseMetric('DPA',analyseFmt(latest.dpa),'FCFA')}${analyseMetric('Marge nette',analyseNum(latest.marge_nette)!==null?Number(latest.marge_nette).toFixed(1)+'%':(analyseNum(latest.chiffre_affaires)&&rn!==null?(rn/Number(latest.chiffre_affaires)*100).toFixed(1)+'%':'—'))}</div>${financialTableForRecommendation(ticker)}</section><section class="tc-client-section"><div class="tc-client-section-title">03 · DERNIERS ÉLÉMENTS PUBLIÉS</div>${publicationSummaryForRecommendation(ticker)}</section><section class="tc-client-section"><div class="tc-client-section-title">04 · MARCHÉ & VALORISATION</div><div class="tc-client-kpi-grid">${analyseMetric('Capitalisation',analyseFmtM(quote.capitalisation),'FCFA')}${analyseMetric('Volume séance',analyseFmtM(quote.volume))}${analyseMetric('52 semaines haut',analyseFmt(quote.plus_haut_52),'FCFA')}${analyseMetric('52 semaines bas',analyseFmt(quote.plus_bas_52),'FCFA')}${analyseMetric('Rendement dividende',analyseNum(latest.dividend_yield)!==null?Number(latest.dividend_yield).toFixed(2)+'%':(analyseNum(latest.dpa)&&ref?((Number(latest.dpa)/ref)*100).toFixed(2)+'%':'—'))}</div></section><footer class="tc-client-foot"><span>Préparé par ${analyseEsc(a.analyste||'The Capital Research')}</span><span>Document généré le ${analyseDate(generated.toISOString())}</span><span>Les données sont celles disponibles dans The Capital au moment de la génération.</span></footer></div>`;}
function openClientRecommendationSheet(id){ensureRecommendationStyles();const rows=Array.isArray(allAnalyses)?allAnalyses:[],a=rows.find(x=>Number(x.id)===Number(id));if(!a)return;const existing=document.getElementById('tcClientSheetOverlay');if(existing)existing.remove();const overlay=document.createElement('div');overlay.id='tcClientSheetOverlay';overlay.className='tc-client-overlay';overlay.innerHTML=`<div class="tc-client-modal"><div class="tc-client-toolbar"><strong>Fiche client</strong><div><button class="filter-btn" type="button" onclick="printClientRecommendation(${Number(id)})">🖨 Imprimer / PDF</button><button class="filter-btn" type="button" onclick="document.getElementById('tcClientSheetOverlay').remove()">Fermer</button></div></div>${analysisClientSheet(a)}</div>`;document.body.appendChild(overlay);}
function printClientRecommendation(id){const rows=Array.isArray(allAnalyses)?allAnalyses:[],a=rows.find(x=>Number(x.id)===Number(id));if(!a)return;const sheet=analysisClientSheet(a),win=window.open('','_blank','noopener,noreferrer,width=1100,height=850');if(!win){toast('Autorisez les fenêtres contextuelles pour exporter la fiche.','warn');return;}const css=Array.from(document.querySelectorAll('link[rel="stylesheet"]')).map(l=>`<link rel="stylesheet" href="${l.href}">`).join('')+'<style>body{background:#fff!important;color:#18140e!important;padding:30px}.tc-client-sheet{max-width:980px;margin:auto;box-shadow:none!important;border:1px solid #ddd!important;color:#18140e!important}.tc-client-toolbar{display:none!important}@media print{body{padding:0}.tc-client-sheet{border:0!important}}</style>';win.document.open();win.document.write(`<!doctype html><html lang="fr"><head><meta charset="utf-8"><title>The Capital — Fiche ${analyseEsc(a.ticker)}</title>${css}</head><body>${sheet}</body></html>`);win.document.close();win.focus();setTimeout(()=>win.print(),500);}
function openAnalyseDetail(id,noHash){ensureRecommendationStyles();const rows=Array.isArray(allAnalyses)?allAnalyses:[],a=rows.find(x=>Number(x.id)===Number(id));if(!a)return;nav('analyse-detail',noHash);if(!noHash)history.replaceState(null,'','#analyse='+id);const ticker=analyseTicker(a.ticker),company=analyseCompany(ticker),quote=analyseQuote(ticker),fins=analyseFinancials(ticker),latest=fins[0]||{},target=analyseTarget(a),ref=analyseReference(a,quote),potential=analysePotential(a,quote),review=analyseReviewFlag(a,quote),sameTicker=rows.filter(x=>analyseTicker(x.ticker)===ticker&&Number(x.id)!==Number(a.id)).sort((x,y)=>Date.parse(y.date_analyse||0)-Date.parse(x.date_analyse||0)),detail=document.getElementById('analyseDetailContent');if(!detail)return;detail.innerHTML=`<div class="tc-rec-detail-head"><div><button class="back-btn" onclick="nav('analyses')">← Retour aux recommandations</button><div class="tc-rec-kicker">THE CAPITAL · RESEARCH NOTE</div><div class="tc-rec-detail-company"><span>${analyseEsc(ticker)}</span><h1>${analyseEsc(company.nom||company.raison_sociale||ticker)}</h1><p>${analyseEsc(company.secteur||company.sous_secteur||'Analyse financière')} · ${analyseDate(a.date_analyse)}</p></div></div><div class="tc-rec-detail-actions">${analyseRecoBadge(a,true)}<button class="filter-btn" onclick="openClientRecommendationSheet(${Number(a.id)})">↗ Fiche client</button></div></div>${review.length?`<div class="tc-rec-warning">⚠ ${analyseEsc(review.join(' · '))}. Cette note doit être réévaluée avant diffusion comme conviction actuelle.</div>`:''}<div class="tc-rec-detail-kpis">${analyseMetric('Cours',ref===null?'—':analyseFmt(ref)+' FCFA',quote.date_seance?'Dernière séance '+analyseDate(quote.date_seance):'')}${analyseMetric('Objectif',target===null?'—':analyseFmt(target)+' FCFA')}${analyseMetric('Potentiel',potential===null?'—':`${potential>=0?'+':''}${potential.toFixed(1)}%`)}${analyseMetric('Résultat net',analyseFmtM(latest.resultat_net),latest.annee?analysePeriodLabel(latest):'—')}</div><div class="tc-rec-detail-grid"><div><section class="tc-rec-panel"><div class="tc-rec-panel-title">01 · THÈSE D’INVESTISSEMENT</div><div class="tc-rec-body">${a.commentaire?analyseEsc(a.commentaire).replace(/\n/g,'<br>'):'Aucune analyse détaillée disponible.'}</div></section><section class="tc-rec-panel"><div class="tc-rec-panel-title">02 · DERNIÈRES DONNÉES FINANCIÈRES PUBLIÉES</div>${financialTableForRecommendation(ticker)}</section><section class="tc-rec-panel"><div class="tc-rec-panel-title">03 · DERNIERS ÉLÉMENTS PUBLIÉS</div>${publicationSummaryForRecommendation(ticker)}</section></div><aside><section class="tc-rec-panel"><div class="tc-rec-panel-title">MARCHÉ</div><div class="tc-rec-side-grid">${analyseMetric('Capitalisation',analyseFmtM(quote.capitalisation))}${analyseMetric('Volume',analyseFmtM(quote.volume))}${analyseMetric('52s haut',analyseFmt(quote.plus_haut_52))}${analyseMetric('52s bas',analyseFmt(quote.plus_bas_52))}</div></section><section class="tc-rec-panel"><div class="tc-rec-panel-title">NOTE</div><div class="tc-rec-side-text"><div><span>Analyste</span><strong>${analyseEsc(a.analyste||'The Capital Research')}</strong></div><div><span>Horizon</span><strong>${analyseEsc(a.horizon||'Non précisé')}</strong></div><div><span>Date</span><strong>${analyseDate(a.date_analyse)}</strong></div></div></section><section class="tc-rec-panel"><div class="tc-rec-panel-title">HISTORIQUE</div>${sameTicker.length?sameTicker.slice(0,5).map(x=>`<button class="tc-rec-history" onclick="openAnalyseDetail(${Number(x.id)})"><strong>${analyseEsc(x.recommandation||'Analyse')}</strong><span>${analyseDate(x.date_analyse)}</span></button>`).join(''):'<div class="tc-rec-empty">Aucune autre note pour cette valeur.</div>'}</section></aside></div>`;}
