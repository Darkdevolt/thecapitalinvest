// THE CAPITAL — Portfolio benchmark, beta and financial flows (frontend only)
(function () {
  'use strict';
  const money=v=>typeof window.fmtM==='function'?window.fmtM(v):Number(v||0).toLocaleString('fr-FR');
  const num=(v,d=2)=>typeof window.fmt==='function'?window.fmt(v,d):Number(v||0).toLocaleString('fr-FR',{minimumFractionDigits:d,maximumFractionDigits:d});
  const txs=()=>typeof window.getTransactions==='function'?window.getTransactions():[];
  const amount=t=>Math.abs(Number(t?.montant_net??t?.montant_brut??t?.amount??((t?.quantite??t?.quantity??t?.qty??0)*(t?.prix_unitaire??t?.price??0))));
  function flowSummary(){let deposits=0,withdrawals=0,sales=0,dividends=0;txs().forEach(t=>{const type=String(t.type||'').toUpperCase(),a=amount(t);if(type==='DEPOT'||type==='DEPOSIT')deposits+=a;else if(type==='RETRAIT'||type==='WITHDRAW')withdrawals+=a;else if(type==='VENTE'||type==='SELL')sales+=a;else if(type==='DIVIDENDE'||type==='DIVIDEND')dividends+=a;});const cash=typeof window.getCash==='function'?Number(window.getCash()||0):Math.max(0,deposits-withdrawals-sales+sales+dividends);return{deposits,withdrawals,sales,dividends,net:deposits-withdrawals,cash};}
  async function addFlow(type){const raw=window.prompt(type==='DEPOT'?'Montant de l’approvisionnement (FCFA)':'Montant du retrait (FCFA)');const a=Number(String(raw||'').replace(/\s/g,'').replace(',','.'));if(!Number.isFinite(a)||a<=0){if(raw!==null&&window.toast)window.toast('Montant invalide.','error');return;}const date=window.prompt('Date (AAAA-MM-JJ)',new Date().toISOString().slice(0,10));if(!date)return;try{await window.portfolioStore.addTransaction({type,amount:a,date,note:type==='DEPOT'?'Approvisionnement du compte':'Retrait du compte'});renderFlows();}catch(e){if(window.toast)window.toast(e.message||'Flux impossible.','error');}}
  function renderFlows(){const anchor=document.getElementById('benchmarkStats');if(!anchor)return;let panel=document.getElementById('portfolioFinancialFlows');if(!panel){panel=document.createElement('div');panel.id='portfolioFinancialFlows';panel.style.cssText='margin-top:16px;border:1px solid var(--border);border-radius:12px;background:var(--panel);overflow:hidden';anchor.parentElement?.parentElement?.insertBefore(panel,anchor.parentElement)||anchor.parentElement?.before(panel);}const s=flowSummary();const investedValue=(Array.isArray(window._pfLastRows)?window._pfLastRows:[]).reduce((x,r)=>x+Number(r.value||0),0);panel.innerHTML=`<div style="padding:16px 18px;border-bottom:1px solid var(--border)"><div style="font-size:11px;letter-spacing:.12em;text-transform:uppercase;color:var(--gold);font-weight:700">Flux financiers</div><div style="font-size:13px;color:var(--dim);margin-top:4px">Approvisionnements/retraits = flux externes. Achats/ventes = mouvements internes.</div></div><div style="display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1px;background:var(--border)"><div style="padding:15px;background:var(--panel2)"><div style="font-size:11px;color:var(--dim)">Approvisionnements</div><strong style="font-size:18px;color:var(--green)">+${money(s.deposits)} FCFA</strong></div><div style="padding:15px;background:var(--panel2)"><div style="font-size:11px;color:var(--dim)">Retraits</div><strong style="font-size:18px;color:var(--red)">−${money(s.withdrawals)} FCFA</strong></div><div style="padding:15px;background:var(--panel2)"><div style="font-size:11px;color:var(--dim)">Capital net injecté</div><strong style="font-size:18px">${money(s.net)} FCFA</strong></div><div style="padding:15px;background:var(--panel2)"><div style="font-size:11px;color:var(--dim)">Capital non alloué</div><strong style="font-size:18px;color:var(--gold)">${money(Math.max(0,s.cash))} FCFA</strong></div></div><div style="display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;padding:14px 16px"><div><span style="color:var(--dim);font-size:12px">Capital investi / titres</span><br><strong>${money(investedValue)} FCFA</strong></div><div><span style="color:var(--dim);font-size:12px">Ventes encaissées</span><br><strong>${money(s.sales)} FCFA</strong><div style="font-size:10px;color:var(--dim)">mouvement interne</div></div><div><span style="color:var(--dim);font-size:12px">Dividendes reçus</span><br><strong style="color:var(--green)">+${money(s.dividends)} FCFA</strong></div></div><div style="display:flex;gap:8px;padding:0 16px 16px;flex-wrap:wrap"><button type="button" id="pfDepositBtn" style="padding:9px 13px;border:1px solid var(--green);border-radius:8px;background:transparent;color:var(--green);cursor:pointer;font-weight:700">+ Approvisionner</button><button type="button" id="pfWithdrawBtn" style="padding:9px 13px;border:1px solid var(--red);border-radius:8px;background:transparent;color:var(--red);cursor:pointer;font-weight:700">− Retirer</button></div>`;document.getElementById('pfDepositBtn')?.addEventListener('click',()=>addFlow('DEPOT'));document.getElementById('pfWithdrawBtn')?.addEventListener('click',()=>addFlow('RETRAIT'));}
  function normalizeRows(payload){return Array.isArray(payload)?payload:(Array.isArray(payload?.data)?payload.data:(Array.isArray(payload?.indices)?payload.indices:(Array.isArray(payload?.data?.indices)?payload.data.indices:[])));}
  function rowDate(r){return String(r?.date_seance??r?.date??r?.date_cotation??r?.jour??'').slice(0,10);}
  function rowClose(r){const n=Number(r?.cours_cloture??r?.cloture??r?.close??r?.cours_normal??r?.cours??r?.valeur??0);return n>0?n:null;}
  async function benchmarkSeries(){const r=await fetch('/api/marche?type=indices',{cache:'no-store'});if(!r.ok)throw new Error('Indice BRVM indisponible');const rows=normalizeRows(await r.json());return rows.filter(x=>{const label=String(x?.ticker??x?.indice??x?.code??x?.nom??'').toUpperCase();return label.includes('BRVM C')||label.includes('COMPOSITE');}).map(x=>({date:rowDate(x),close:rowClose(x)})).filter(x=>x.date&&x.close).sort((a,b)=>a.date.localeCompare(b.date));}
  const compounded=rs=>rs.reduce((p,r)=>p*(1+r),1)-1;
  const calcBeta=(p,b)=>{const n=Math.min(p.length,b.length);if(n<2)return null;const pp=p.slice(-n),bb=b.slice(-n),mp=pp.reduce((s,x)=>s+x,0)/n,mb=bb.reduce((s,x)=>s+x,0)/n;let cov=0,v=0;for(let i=0;i<n;i++){cov+=(pp[i]-mp)*(bb[i]-mb);v+=(bb[i]-mb)**2;}return v>0?cov/v:null;};
  async function renderBenchmarkReal(){const el=document.getElementById('benchmarkStats');if(!el)return;const hist=typeof window.getPortfolioHistory==='function'?window.getPortfolioHistory(window._pfPeriod||99999):null;if(!hist?.dates?.length){el.innerHTML='<div style="padding:16px"><div style="font-size:11px;color:var(--dim)">Performance vs BRVM Composite</div><strong>—</strong><div style="font-size:12px;color:var(--dim);margin-top:6px">Historique du portefeuille insuffisant.</div></div>';return;}try{const idx=await benchmarkSeries(),map=new Map(idx.map(x=>[x.date,x.close])),dates=hist.dates.map(d=>new Date(d).toISOString().slice(0,10)),pRet=[],bRet=[];for(let i=1;i<dates.length;i++){const d0=dates[i-1],d1=dates[i];if(!map.has(d0)||!map.has(d1))continue;const v0=Number(hist.values[i-1]),v1=Number(hist.values[i]),b0=map.get(d0),b1=map.get(d1);if(v0>0&&v1>0&&b0>0&&b1>0){pRet.push(v1/v0-1);bRet.push(b1/b0-1);}}if(pRet.length<2)throw new Error('Pas assez de rendements communs pour le benchmark.');const portfolio=compounded(pRet)*100,benchmark=compounded(bRet)*100,excess=portfolio-benchmark,beta=calcBeta(pRet,bRet);el.innerHTML=`<div style="padding:16px"><div style="font-size:11px;color:var(--dim);margin-bottom:10px">Performance vs BRVM Composite</div><div style="display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px"><div><div style="font-size:11px;color:var(--dim)">Portefeuille</div><strong style="font-size:20px;color:${portfolio>=0?'var(--green)':'var(--red)'}">${portfolio>=0?'+':''}${num(portfolio,2)}%</strong></div><div><div style="font-size:11px;color:var(--dim)">BRVM Composite</div><strong style="font-size:20px">${benchmark>=0?'+':''}${num(benchmark,2)}%</strong></div><div><div style="font-size:11px;color:var(--dim)">Surperformance</div><strong style="font-size:20px;color:${excess>=0?'var(--green)':'var(--red)'}">${excess>=0?'+':''}${num(excess,2)}%</strong></div><div><div style="font-size:11px;color:var(--dim)">Bêta</div><strong style="font-size:20px">${beta==null?'—':num(beta,2)}</strong></div></div><div style="font-size:11px;color:var(--dim);margin-top:10px">Même période et séances communes. Dépôts/retraits exclus de la performance.</div></div>`;}catch(e){el.innerHTML=`<div style="padding:16px"><div style="font-size:11px;color:var(--dim)">Performance vs BRVM Composite</div><strong>—</strong><div style="font-size:12px;color:var(--dim);margin-top:6px">${e.message}</div></div>`;}}
  function correctHeadlineMetrics(){const rows=Array.isArray(window._pfLastRows)?window._pfLastRows:[];if(!rows.length)return;const ts=txs(),net=ts.reduce((s,t)=>{const type=String(t.type||'').toUpperCase(),a=amount(t);return type==='DEPOT'||type==='DEPOSIT'?s+a:type==='RETRAIT'||type==='WITHDRAW'?s-a:s;},0),dividends=ts.reduce((s,t)=>{const type=String(t.type||'').toUpperCase();return type==='DIVIDENDE'||type==='DIVIDEND'?s+amount(t):s;},0),invested=rows.reduce((s,r)=>s+Number(r.invested||0),0),pnl=rows.reduce((s,r)=>s+Number(r.pl||0),0),rentability=invested>0?pnl/invested*100:0,dividendYield=net>0?dividends/net*100:0;const pfReturn=document.getElementById('pfReturn'),pfPLSub=document.getElementById('pfPLSub');if(pfReturn){pfReturn.textContent=`${num(dividendYield,2)}%`;pfReturn.style.color='var(--green)';}if(pfPLSub){pfPLSub.textContent=`${rentability>=0?'+':''}${num(rentability,2)}% de rentabilité`;pfPLSub.style.color=rentability>=0?'var(--green)':'var(--red)';}}
  window.renderPortfolioBenchmarkAndFlows=()=>{renderFlows();renderBenchmarkReal();correctHeadlineMetrics();};
  const run=()=>{try{window.renderPortfolioBenchmarkAndFlows();}catch(e){console.warn('[PORTFOLIO BENCHMARK/FLOWS]',e);}};
  window.addEventListener('portfolio:updated',run);window.addEventListener('portfolio:history-ready',run);window.addEventListener('dataLoaded',run);setTimeout(run,700);
})();
