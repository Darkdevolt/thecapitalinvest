# Core application

Responsabilités : démarrage de l’application, routage, état global et événements communs.

Les modules métier ne doivent pas être chargés depuis ce dossier. Ils doivent passer par `services/`, `components/` ou `views/`.
