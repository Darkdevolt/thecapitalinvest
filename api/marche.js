import { supabase, supabaseAdmin } from '../lib/supabase.js';

const db = supabaseAdmin || supabase;
function json(res,status,payload){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store, no-cache, must-revalidate, proxy-revalidate');res.setHeader('Pragma','no-cache');res.setHeader('Expires','0');res.end(JSON.stringify(payload));}
async function query(table,build){if(!db)throw new Error('Supabase non configuré');let q=db.from(table).select('*');if(typeof build==='function')q=build(q);return q;}
export default async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'Method Not Allowed'});try{const url=new URL(req.url,`https://${req.headers.host||'localhost'}`);const type=url.searchParams.get('type')||'cours';const ticker=(url.searchParams.get('ticker')||'').trim().toUpperCase();const limit=Math.min(Math.max(Number(url.searchParams.get('limit'))||30,1),1000);let result;switch(type){case'cours':result=await query('cours_latest',q=>q.order('ticker',{ascending:true}));break;case'indices':result=await query('indices',q=>q.order('date_seance',{ascending:false}).limit(1000));break;case'historique':if(!ticker)return json(res,400,{error:'ticker requis'});result=await query('historique',q=>q.eq('ticker',ticker).order('date_seance',{ascending:false}).limit(limit));if(!result.error&&Array.isArray(result.data))result.data.reverse();break;case'entreprises':result=await query('entreprises',q=>q.eq('actif',true).order('ticker',{ascending:true}));break;case'financials':result=await query('financials',q=>q.order('validation_status',{ascending:true}).order('annee',{ascending:false}).limit(2000));break;case'analyses':result=await query('analyses',q=>q.order('date_analyse',{ascending:false}).limit(500));break;case'apercu':{const [cours,indices]=await Promise.all([query('cours_latest',q=>q.order('ticker',{ascending:true})),query('indices',q=>q.order('date_seance',{ascending:false}).limit(100))]);if(cours.error)throw cours.error;if(indices.error)throw indices.error;return json(res,200,{success:true,cours:cours.data||[],indices:indices.data||[]});}default:return json(res,400,{error:`Type de données inconnu: ${type}`});}if(result.error)throw result.error;return json(res,200,result.data||[]);}catch(error){console.error('[API/MARCHE]',error);return json(res,500,{error:error.message||'Erreur serveur'});}}
