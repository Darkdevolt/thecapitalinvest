name: Apply landing enhancement
on:
  push:
    paths-ignore:
      - 'public/index.html'
permissions:
  contents: write
jobs:
  enhance:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired: public/index.html is now the canonical self-contained landing.'
