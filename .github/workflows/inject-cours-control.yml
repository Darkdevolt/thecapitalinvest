name: Legacy workflow disabled
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy injection disabled: CoursControl is now loaded explicitly from admin.html"
